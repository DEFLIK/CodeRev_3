import { CodeEditorDirective } from './code-editor.directive';

describe('CodeEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeEditorDirective();
    expect(directive).toBeTruthy();
  });
});
